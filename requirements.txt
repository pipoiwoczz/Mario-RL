# Core
numpy==1.24.3
opencv-python==4.10.0.84
imageio==2.34.1

# Mario env (nes-py based; sticks to Gym <=0.25.x)
gym==0.25.1
gym-super-mario-bros==7.4.0
nes-py==8.2.1
# (If you later migrate to Gymnasium, keep these separate in an extras)

# RL (PyTorch stack)
torch>=2.1,<2.4
tensorboard>=2.14,<2.18
